fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.8.2
python-dotenv==1.0.1
httpx==0.27.2

# LLM SDKs (used directly in llm_reasoner.py + llm_client.py)
openai==1.51.0

# LangGraph 1.x — for the agentic review graph (interrupt/Command primitives)
# langchain-core comes transitively from langgraph; don't pin here, avoids resolver conflicts.
langgraph==1.1.9

# Knowledge base (RAG retrieval). ChromaDB ships its own ONNX embedder by default;
# if it can't load on a slim deploy, the KB transparently falls back to BM25.
chromadb==0.5.20

# LangSmith tracing — optional, activated when LANGSMITH_API_KEY is set
langsmith==0.1.147

# Follow-up agent
apscheduler==3.10.4
twilio==9.3.2
